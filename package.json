{
  "name": "lt-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:harness": "playwright test tests/harness.spec.ts",
    "test:ss:soak": "node scripts/sacred-stones-soak.mjs",
    "bundle": "node scripts/bundle-assets.mjs",
    "generate-icons": "node scripts/generate-icons.mjs",
    "setup:capacitor": "node scripts/setup-native.mjs capacitor",
    "setup:twa": "node scripts/setup-native.mjs twa",
    "cap:sync": "npx cap sync",
    "cap:ios": "npx cap open ios",
    "cap:android": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@playwright/test": "^1.58.2",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "node-unrar-js": "^2.0.2"
  }
}
